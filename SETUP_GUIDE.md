# دليل الإعداد السريع

## ✅ تم إنشاء المشروع بنجاح!

متجر مستحضرات التجميل جاهز للاستخدام الآن.

## 🚀 الخطوات التالية

### 1. إنشاء حساب Admin
لتسجيل الدخول إلى لوحة تحكم Filament، قم بإنشاء حساب:

```bash
php artisan make:filament-user
```

أدخل المعلومات التالية:
- **الاسم**: Admin
- **البريد الإلكتروني**: admin@yourdomain.com
- **كلمة المرور**: اختر كلمة مرور قوية

### 2. الوصول إلى المشروع

#### الواجهة الأمامية (المتجر)
🌐 http://localhost:8000

هنا يمكن للعملاء:
- تصفح المنتجات
- إضافة منتجات للسلة
- إتمام الطلبات
- عرض طلباتهم (بعد تسجيل الدخول)

#### لوحة التحكم Filament (الإدارة)
🔐 http://localhost:8000/admin

هنا يمكنك:
- إضافة وتعديل الفئات
- إدارة المنتجات
- رفع صور المنتجات
- متابعة الطلبات
- تغيير حالات الطلبات

### 3. البيانات التجريبية

تم إضافة بيانات تجريبية تشمل:
- ✅ 4 فئات رئيسية
- ✅ 16 منتج متنوع
- ✅ مستخدم Admin (إذا قمت بتشغيل الـ Seeder)

**مستخدم Admin التجريبي**:
- البريد: admin@example.com
- كلمة المرور: password

> ⚠️ **تنبيه أمني**: تأكد من تغيير كلمة المرور قبل النشر!

## 📝 المهام الأولى

### إضافة منتجات حقيقية
1. سجل الدخول إلى `/admin`
2. انتقل إلى Products
3. اضغط "Create"
4. املأ البيانات وارفع الصور
5. احفظ المنتج

### رفع صور المنتجات
- الصور يجب أن تكون بجودة عالية
- الصيغ المدعومة: JPG, PNG, WEBP
- الحجم الموصى به: 800x800 بكسل
- يمكنك رفع عدة صور لكل منتج

### إعداد معلومات المتجر
قم بتعديل المعلومات في:
- `resources/views/layouts/app.blade.php` - اسم المتجر والشعار
- Footer - معلومات التواصل

## 💰 تفعيل الدفع الإلكتروني

### الدفع عند الاستلام (مفعّل حالياً)
✅ جاهز ويعمل بشكل افتراضي

### Stripe (جاهز للتفعيل)
1. سجل في https://stripe.com
2. احصل على API Keys
3. أضفها في `.env`:
```env
STRIPE_KEY=pk_test_...
STRIPE_SECRET=sk_test_...
```
4. في `/resources/views/checkout/index.blade.php`:
   - احذف `disabled` من input الـ Stripe
   - احذف `opacity-50 cursor-not-allowed` من الـ classes

### PayPal (جاهز للتفعيل)
1. سجل في https://developer.paypal.com
2. أضف المفاتيح في `.env`:
```env
PAYPAL_CLIENT_ID=...
PAYPAL_SECRET=...
PAYPAL_MODE=sandbox
```
3. نفس الخطوات السابقة لإزالة `disabled`

## 🎨 التخصيص

### تغيير الألوان
عدّل في `resources/views/layouts/app.blade.php`:
```css
.gradient-bg {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### إضافة رسوم شحن
في `app/Http/Controllers/CheckoutController.php` السطر 70:
```php
$shippingCost = 50000; // بدلاً من 0
```

### تغيير عملة العرض
استبدل "ل.س" بعملتك في جميع الملفات:
```bash
grep -r "ل.س" resources/views/
```

## 📱 للنشر على سيرفر حقيقي

1. **رفع الملفات**
   - ارفع جميع الملفات ما عدا `/node_modules` و `/vendor`

2. **على السيرفر**
```bash
composer install --optimize-autoloader --no-dev
php artisan key:generate
php artisan migrate --force
php artisan storage:link
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

3. **الأمان**
   - غيّر `APP_ENV=production` في `.env`
   - غيّر `APP_DEBUG=false`
   - استخدم HTTPS
   - فعّل Firewall

## 🆘 حل المشاكل الشائعة

### الصور لا تظهر
```bash
php artisan storage:link
chmod -R 775 storage
chmod -R 775 bootstrap/cache
```

### خطأ 500
```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

### الأنماط لا تعمل
تأكد من تشغيل:
```bash
npm install
npm run build
```

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. تحقق من ملف `storage/logs/laravel.log`
2. تأكد من أن جميع الـ requirements متوفرة
3. راجع الـ README الرئيسي

---

🎉 **مبروك! متجرك جاهز للانطلاق**

البيانات الحالية تجريبية - ابدأ بإضافة منتجاتك الحقيقية الآن!
