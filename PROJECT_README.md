# متجر مستحضرات التجميل - Cosmetics Store

متجر إلكتروني متكامل لبيع مستحضرات التجميل مبني باستخدام Laravel 12 مع لوحة تحكم Filament.

## المميزات الرئيسية

### الواجهة الأمامية (للعملاء)
- ✅ تصميم عصري وجذاب باستخدام Tailwind CSS
- ✅ عرض المنتجات حسب الفئات
- ✅ صفحة تفاصيل المنتج مع صور متعددة
- ✅ سلة تسوق ديناميكية
- ✅ نظام الطلبات مع معلومات التوصيل
- ✅ الدفع عند الاستلام (افتراضي)
- ✅ صفحات العميل لعرض الطلبات وتفاصيلها
- ✅ دعم تسجيل الدخول البسيط للعملاء
- ✅ سلة تسوق للزوار (Session-based)

### لوحة التحكم Filament (للإدارة)
- ✅ إدارة الفئات والمنتجات
- ✅ إدارة الطلبات وحالاتها
- ✅ رفع صور متعددة للمنتجات
- ✅ نظام المخزون
- ✅ تتبع حالات الطلبات

### نظام الدفع
- ✅ الدفع عند الاستلام (مفعّل)
- 🔄 دعم Stripe (جاهز للتفعيل)
- 🔄 دعم PayPal (جاهز للتفعيل)

## التثبيت والإعداد

### المتطلبات
- PHP 8.2+
- Composer
- MySQL/MariaDB
- Node.js & NPM

### خطوات التثبيت

1. **تثبيت المكتبات**
```bash
composer install
npm install
```

2. **إعداد ملف البيئة**
```bash
cp .env.example .env
php artisan key:generate
```

3. **إعداد قاعدة البيانات**
عدّل ملف `.env` وأضف بيانات قاعدة البيانات:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=newstore
DB_USERNAME=root
DB_PASSWORD=
```

4. **تشغيل الـ Migrations**
```bash
php artisan migrate
```

5. **إضافة البيانات التجريبية**
```bash
php artisan db:seed --class=ShopSeeder
```

6. **إنشاء مستخدم Admin لـ Filament**
```bash
php artisan make:filament-user
```
- البريد: admin@example.com
- كلمة المرور: password

7. **تشغيل المشروع**
```bash
php artisan serve
npm run dev
```

## الروابط المهمة

- **الواجهة الأمامية**: http://localhost:8000
- **لوحة تحكم Filament**: http://localhost:8000/admin
  - البريد: admin@example.com
  - كلمة المرور: password

## هيكل المشروع

### Models
- `Category` - فئات المنتجات
- `Product` - المنتجات
- `Cart` - سلة التسوق
- `CartItem` - محتويات السلة
- `Order` - الطلبات
- `OrderItem` - محتويات الطلب

### Controllers
- `ShopController` - صفحات المتجر الرئيسية
- `CartController` - إدارة سلة التسوق
- `CheckoutController` - إتمام الطلبات

### Routes
```
GET  /                     - الصفحة الرئيسية
GET  /product/{slug}       - تفاصيل المنتج
GET  /category/{slug}      - منتجات الفئة
GET  /cart                 - سلة التسوق
POST /cart/add/{product}   - إضافة للسلة
GET  /checkout             - صفحة الدفع
POST /checkout             - إتمام الطلب
GET  /my-orders            - طلبات العميل (يتطلب تسجيل دخول)
```

## تفعيل بوابات الدفع الإلكتروني

### Stripe
1. قم بالتسجيل في [Stripe](https://stripe.com)
2. احصل على API Keys
3. أضف المفاتيح في `.env`:
```env
STRIPE_KEY=your_publishable_key
STRIPE_SECRET=your_secret_key
```
4. قم بإزالة `disabled` من زر Stripe في صفحة Checkout
5. أنشئ controller لمعالجة الدفع

### PayPal
1. قم بالتسجيل في [PayPal Developer](https://developer.paypal.com)
2. احصل على Client ID و Secret
3. أضف المفاتيح في `.env`:
```env
PAYPAL_CLIENT_ID=your_client_id
PAYPAL_SECRET=your_secret
PAYPAL_MODE=sandbox
```
4. قم بإزالة `disabled` من زر PayPal في صفحة Checkout
5. أنشئ controller لمعالجة الدفع

## إضافة صور المنتجات

1. ارفع الصور عبر لوحة تحكم Filament
2. سيتم حفظ الصور في `storage/app/public/`
3. تأكد من ربط مجلد التخزين:
```bash
php artisan storage:link
```

## التخصيص

### تغيير الألوان
عدّل الألوان في ملف `resources/views/layouts/app.blade.php`:
```css
.gradient-bg {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### تغيير اسم المتجر
عدّل في `resources/views/layouts/app.blade.php`:
```html
<span class="text-2xl font-bold gradient-text">متجر الجمال</span>
```

### إضافة رسوم الشحن
عدّل في `CheckoutController@store`:
```php
$shippingCost = 50000; // 50,000 ل.س
```

## الأمان

- ✅ حماية CSRF
- ✅ تشفير كلمات المرور
- ✅ Validation للمدخلات
- ✅ حماية المسارات بـ Middleware
- ✅ التحقق من الصلاحيات

## الدعم الفني

للمساعدة أو الاستفسارات:
- البريد الإلكتروني: support@example.com
- الهاتف: +963 XXX XXX XXX

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

تم التطوير بواسطة Laravel 12 ❤️
